spring:
  application:
    name: notification-service

  mail:
    host: smtp.gmail.com
    port: 587
    username: rajasekharreddymits@gmail.com
    password: kpyx scuk flty lxtc
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

server:
  port: 9020

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: notification-service
